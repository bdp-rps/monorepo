import * as React from 'react'

import { Intakt } from '@bdp-rps/shared'

import Layout from '../../components/Layout'
import Template from '../../components/Template'

export default () => (
  <Template>
    <Layout paddingTop={5} paddingBottom={10}>
      <Intakt />
    </Layout>
  </Template>
)
