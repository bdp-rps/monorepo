import * as React from 'react'

import { Landesverband } from '@bdp-rps/shared'

import Layout from '../../components/Layout'
import Template from '../../components/Template'

export default () => (
  <Template>
    <Layout paddingTop={10} paddingBottom={15}>
      <Landesverband />
    </Layout>
  </Template>
)
