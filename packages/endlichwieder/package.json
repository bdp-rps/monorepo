{
  "name": "endlichwieder",
  "version": "0.0.25",
  "description": "endlichwieder.pfadfinden.de",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "yarn images:build && next build",
    "images:build": "node scripts/build-images.js",
    "images:optimize": "sh scripts/optimize-images.sh",
    "images:rotate": "sh scripts/rotate-images.sh",
    "start": "next start",
    "export": "rimraf out && next export",
    "prepare": "echo 1"
  },
  "dependencies": {
    "@bdp-rps/ui": "^0.0.25",
    "@next/bundle-analyzer": "9.3.4",
    "next": "^12.0.7",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@squoosh/cli": "^0.6.4"
  }
}
