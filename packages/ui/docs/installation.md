First of all make sure to install [Yarn](https://yarnpkg.com) as a better alternative to npm.

```sh
curl -o- -L https://yarnpkg.com/install.sh | bash
```

Now run yarn to install the dependencies and build all packages.

```sh
yarn
```
